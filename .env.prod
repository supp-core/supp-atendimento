# Variáveis de ambiente para produção

# Senha do banco de dados (substitua por uma senha forte)
DB_PASSWORD=StrongProductionPasswordHere123!

# Configuração JWT (gere uma nova frase de acesso)
JWT_PASSPHRASE=SomeSecureJWTPassphraseHere

# Configuração de e-mail (substitua pelo seu servidor SMTP real)
MAILER_HOST=smtp.example.com
MAILER_PORT=587
MAILER_USER=notifications@yourdomain.com
MAILER_PASSWORD=YourMailPasswordHere

# Domínio para CORS
CORS_ALLOW_ORIGIN='^https://yourdomain\.com$'

# URLs de API para o frontend
VUE_APP_API_URL=https://yourdomain.com